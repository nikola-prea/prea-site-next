import { css } from 'styled-components';
import './preaStyles.css';

/**
 *
 * Override CSS from external components (e.g. when using an external component library or plugin)
 */
const CSSOverride = css``;

export default CSSOverride;
